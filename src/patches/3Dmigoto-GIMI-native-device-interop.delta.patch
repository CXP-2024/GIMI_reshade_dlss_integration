diff --git a/DirectX11/DirectX11.vcxproj b/DirectX11/DirectX11.vcxproj
index a968f69..8795704 100644
--- a/DirectX11/DirectX11.vcxproj
+++ b/DirectX11/DirectX11.vcxproj
@@ -463,7 +463,7 @@ $(SolutionDir)D3D_Shaders;
     </ClCompile>
     <Link>
       <SubSystem>Windows</SubSystem>
-      <GenerateDebugInformation>true</GenerateDebugInformation>
+      <GenerateDebugInformation>false</GenerateDebugInformation>
       <EnableCOMDATFolding>true</EnableCOMDATFolding>
       <OptimizeReferences>true</OptimizeReferences>
       <ModuleDefinitionFile>d3d11Wrapper.def</ModuleDefinitionFile>
@@ -547,4 +547,4 @@ xcopy "$(SolutionDir)Dependencies\*.*" "$(TargetDir)"  /E /Y /S
   <Import Project="$(VCTargetsPath)\Microsoft.Cpp.targets" />
   <ImportGroup Label="ExtensionTargets">
   </ImportGroup>
-</Project>
\ No newline at end of file
+</Project>
diff --git a/DirectX11/HackerDevice.cpp b/DirectX11/HackerDevice.cpp
index 3b38e6d..f7246d4 100644
--- a/DirectX11/HackerDevice.cpp
+++ b/DirectX11/HackerDevice.cpp
@@ -1617,6 +1617,16 @@ HRESULT STDMETHODCALLTYPE HackerDevice::QueryInterface(
 		return S_OK;
 	}
 
+	if (ppvObject && IsEqualIID(riid, IID_GimiPassThroughDevice)) {
+		// OptiScaler's private D3D11-on-D3D12 bridge must initialise native
+		// NGX and create shared resources with the original device. Returning
+		// it through a dedicated IID keeps the game's ordinary calls on GIMI.
+		mOrigDevice1->AddRef();
+		*ppvObject = mOrigDevice1;
+		LogInfo("  return GIMI pass-through device %p for private interop\n", mOrigDevice1);
+		return S_OK;
+	}
+
 	HRESULT hr = mOrigDevice1->QueryInterface(riid, ppvObject);
 	if (FAILED(hr))
 	{
diff --git a/DirectX11/HackerDevice.h b/DirectX11/HackerDevice.h
index 32062fb..36b2b67 100644
--- a/DirectX11/HackerDevice.h
+++ b/DirectX11/HackerDevice.h
@@ -13,6 +13,12 @@
 DEFINE_GUID(IID_HackerDevice,
 0x83ffd841, 0xa5c9, 0x46f4, 0x81, 0x9, 0xbc, 0x25, 0x95, 0x58, 0xfe, 0xf4);
 
+// Private interop contract for components that need the original D3D11
+// device without bypassing GIMI for normal game rendering.
+// {DB17DC9A-5A5A-4AC7-A4CE-EF41F7C51D5C}
+DEFINE_GUID(IID_GimiPassThroughDevice,
+0xdb17dc9a, 0x5a5a, 0x4ac7, 0xa4, 0xce, 0xef, 0x41, 0xf7, 0xc5, 0x1d, 0x5c);
+
 // Forward declaration to allow circular reference between HackerContext and HackerDevice. 
 // We need this to allow each to reference the other as needed.
 
